<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Rainforest</title>
  </head>
  <body>
    <h1>Products</h1>
    <%= link_to 'New', new_product_path %>
    <% @products.each do |product| %>
      <p>Name: <%= product.name %></p>
      <p>Description: <%= product.description %></p>
      <p>Price(¢): <%= product.price_in_cents %></p>
      <h5><%= link_to "View", "/products/#{product.id}" %> | <%= link_to "Edit", "/products/#{product.id}/edit" %> | <%= link_to "Delete", @product, method: :delete, data: {confirm: "Are you sure you want to delete this product?"} %><h5>
      <% end %>

  </body>
</html>
