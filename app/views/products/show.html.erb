<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Rainforest</title>
  </head>
  <body>
    <h1><%= @product.name %></h1>
    <%= link_to "Home", products_path %> | <%= link_to "Delete", @product, method: :delete, data: {confirm: "Are you sure you want to delete this product?"} %>
      <p><%= @product.name %> <%= @product.description %>
      <p><%= @product.price_in_cents %>
  </body>
</html>
